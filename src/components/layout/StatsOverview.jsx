/**
 * StatsOverview – RETIRED
 *
 * This component has been superseded by the 4-column KPI bar
 * embedded directly in App.jsx (Dashboard component).
 * The logic is computed via useMemo from useCalendar().events.
 *
 * This file is kept as a stub to avoid broken imports from
 * any other branch/component that might reference it.
 */
export default function StatsOverview() {
  return null;
}
